import React from 'react';
import {FontAwesomeIcon} from '@fortawesome/react-fontawesome'

const Loading = () => (
  <div><FontAwesomeIcon icon={["fa", "spinner"]} color="#de3a3a"/></div>
)

export default Loading;